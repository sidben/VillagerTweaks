package sidben.villagertweaks.init;



public class MyRecipes
{
    
    public static void register() {

        // Furnace recipe
        /*
        GameRegistry.addSmelting(Blocks.diamond_block, new ItemStack(Items.diamond, 0, 0), 1.0F);   // Fake villager smelt recipe (kinda work, but not really)
        GameRegistry.addSmelting(Blocks.gold_block, null, 1.0F);   // Fake villager smelt recipe (don't work)
        */
    }

}
